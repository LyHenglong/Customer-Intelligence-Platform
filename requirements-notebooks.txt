# Analysis-only dependencies: needed to re-run notebooks/, nothing else.
#
# Deliberately kept OUT of requirements.txt, which is installed into the
# API image, the dashboard image, and the CI runner. None of those three
# import anything below - adding ~400MB of plotting and modeling libraries
# to every container to support code that only ever runs on a laptop would
# be a real cost for no runtime benefit, and would slow every CI run.
#
# Install on top of the base requirements:
#     pip install -r requirements.txt -r requirements-notebooks.txt
#
# Versions are the ones the committed notebook outputs were actually
# produced with, so re-running against these reproduces the numbers in
# report/findings.md rather than approximately reproducing them.

# Statistical testing and survival analysis
statsmodels==0.15.0        # variance inflation factors (multicollinearity)
lifelines==0.30.0          # Kaplan-Meier, Cox proportional hazards

# Model comparison experiment (notebooks/model_dev_offline.py)
xgboost==2.1.3             # one of the 4 families compared
imbalanced-learn==0.12.4   # SMOTE, evaluated against class_weight="balanced"

# Plotting
matplotlib==3.10.9
seaborn==0.13.2

# Running the .ipynb files
jupyter==1.1.1
ipykernel==7.3.0
