# Copy to .env.local for local dev (gitignored by create-next-app's default .gitignore).
# Build-time-inlined into the browser bundle - see lib/api-client.ts's comment.
NEXT_PUBLIC_API_URL=http://localhost:8000
